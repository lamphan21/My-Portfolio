
  # Add responsive hover effects

  This is a code bundle for Add responsive hover effects. The original project is available at https://www.figma.com/design/uOrvaIRaCNd1IdtF8H9qrM/Add-responsive-hover-effects.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  